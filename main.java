public class main {
    public static void main(String[] args) {
        // Create a new instance of the LogicalBuilding class
        LogicalBuilding building = new LogicalBuilding();

        // Call the method to display the building's information
        building.displayBuildingInfo();
    }
}