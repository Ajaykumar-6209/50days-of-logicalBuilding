public class LogicalBuilding {
    public static void main(String[] args) {
        // Create a new instance of the TreeSet class
        java.util.TreeSet<String> treeSet = new java.util.TreeSet<String>();

        // Add elements to the TreeSet
        treeSet.add("mango");
        treeSet.add("banana");
        treeSet.add("apple");
        treeSet.add("kiwi fruit");
        treeSet.add("orange");
        treeSet.add("grape");

        // Iterate through the TreeSet and print each element
        for (String fruit : treeSet) {
            System.out.println(fruit);

            treeset.get(1);
            System.out.println("The second element is: " + treeset.get(1));
            treeser.set(1, "pineapple");
            System.out.println("After updating, the second element is: " + treeset.get(1));
        }

        
    }
   
}